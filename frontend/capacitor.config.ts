import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gestion.tareas',
  appName: 'GestionTareas',
  webDir: 'dist/frontend/browser',
  
};

export default config;
